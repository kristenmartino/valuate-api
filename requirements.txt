fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.5
langgraph>=0.2,<1.0
anthropic>=0.50
beautifulsoup4>=4.12
lxml>=5.0

# Test-only; kept here so a fresh clone gets to a runnable `pytest tests/` in
# one step. Tiny footprint, fine to include in production install.
pytest>=7
